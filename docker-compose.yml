services:
  affiliate-studio:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      - ./data:/app/data
      - ./public/generated:/app/public/generated
    restart: unless-stopped
