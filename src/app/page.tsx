"use client";

import { useState } from "react";
import { Sparkles, Video, Package, Wand2 } from "lucide-react";

export default function Home() {
  const [url, setUrl] = useState("");
  const [name, setName] = useState("");
  const [description, setDescription] = useState("");
  const [variants, setVariants] = useState(3);
  const [tone, setTone] = useState<"viral"|"review"|"educational"|"story">("viral");
  const [status, setStatus] = useState("");

  async function generate() {
    setStatus("Đang tạo nội dung…");
    const res = await fetch("/api/content/generate", { method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({ product:{ id:crypto.randomUUID(), name:name||"Sản phẩm mới", url, description:description||"Chưa có mô tả", features:[], createdAt:new Date().toISOString() }, variants, tone }) });
    const data = await res.json();
    setStatus(res.ok ? `Đã tạo ${data.variants.length} kịch bản. Bước tiếp theo là render video.` : data.error);
  }

  return <main className="min-h-screen"><header className="border-b border-zinc-800 px-8 py-5 flex items-center justify-between"><div className="flex items-center gap-3"><div className="rounded-xl bg-white text-black p-2"><Sparkles size={20}/></div><div><div className="font-bold">Affiliate Studio</div><div className="text-xs text-zinc-500">AI Content Automation</div></div></div><div className="text-sm text-zinc-400">Products · Content · Videos · Analytics</div></header><div className="mx-auto max-w-6xl px-6 py-12"><div className="max-w-3xl"><div className="text-sm text-zinc-400 mb-3">AFFILIATE CONTENT ENGINE</div><h1 className="text-5xl font-bold tracking-tight">Biến sản phẩm thành <span className="text-zinc-400">video bán hàng.</span></h1><p className="mt-5 text-lg text-zinc-400">Nhập sản phẩm một lần. AI tạo nhiều góc nội dung, hook, kịch bản và CTA để bạn sản xuất video TikTok hàng loạt.</p></div><section className="card mt-10 p-6"><div className="flex items-center gap-2 font-semibold"><Package size={18}/> Product intake</div><div className="grid md:grid-cols-2 gap-5 mt-6"><label className="text-sm text-zinc-400">Product URL<input value={url} onChange={e=>setUrl(e.target.value)} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-950 px-4 py-3 outline-none" placeholder="https://..."/></label><label className="text-sm text-zinc-400">Tên sản phẩm<input value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-950 px-4 py-3 outline-none" placeholder="Ví dụ: Máy hút bụi mini"/></label><label className="md:col-span-2 text-sm text-zinc-400">Mô tả / điểm nổi bật<textarea value={description} onChange={e=>setDescription(e.target.value)} className="mt-2 min-h-28 w-full rounded-xl border border-zinc-700 bg-zinc-950 px-4 py-3 outline-none" placeholder="Nhập thông tin sản phẩm nếu chưa có URL..."/></label></div></section><section className="grid md:grid-cols-3 gap-4 mt-5"><div className="card p-5"><div className="text-sm muted">Video variants</div><select value={variants} onChange={e=>setVariants(Number(e.target.value))} className="mt-3 w-full rounded-lg bg-zinc-900 p-3"><option value={1}>1 variant</option><option value={3}>3 variants</option><option value={5}>5 variants</option><option value={10}>10 variants</option></select></div><div className="card p-5"><div className="text-sm muted">Content angle</div><select value={tone} onChange={e=>setTone(e.target.value as typeof tone)} className="mt-3 w-full rounded-lg bg-zinc-900 p-3"><option value="viral">Viral hook</option><option value="review">Review</option><option value="educational">Educational</option><option value="story">Story</option></select></div><div className="card p-5 flex flex-col justify-between"><div><div className="text-sm muted">Automation</div><div className="mt-3 font-medium">AI script generation</div></div><button onClick={generate} className="mt-5 rounded-xl bg-white text-black px-5 py-3 font-semibold flex items-center justify-center gap-2"><Wand2 size={17}/> Generate content</button></div></section>{status && <div className="mt-5 rounded-xl border border-zinc-800 p-4 text-sm text-zinc-300">{status}</div>}<section className="mt-12 grid md:grid-cols-3 gap-4"><Feature icon={<Sparkles/>} title="AI Content" text="Hook, script, CTA, caption và hashtag theo từng góc bán hàng."/><Feature icon={<Video/>} title="Video Engine" text="Kiến trúc Remotion/FFmpeg sẵn sàng cho render 9:16 hàng loạt."/><Feature icon={<Package/>} title="Batch" text="Một sản phẩm có thể tạo nhiều biến thể để A/B test."/></section></div></main>
}
function Feature({icon,title,text}:{icon:React.ReactNode;title:string;text:string}) { return <div className="card p-5"><div className="text-zinc-400">{icon}</div><h3 className="mt-4 font-semibold">{title}</h3><p className="mt-2 text-sm text-zinc-500 leading-6">{text}</p></div> }
